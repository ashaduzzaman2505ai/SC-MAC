# configs/config.yaml
defaults:
  - _self_
  - agent: worker_agent
  - model: llama3_8b
  - experiment: baseline_fed

run_name: "sc_mac_v1"
seed: 42
device: "cuda"

federated:
  num_rounds: 50
  clients_per_round: 5
  aggregation: "truth_weighted" # Our novel SC-MAC method